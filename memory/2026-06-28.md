# 2026-06-28 — IIAU-778 Bug Quill Editor

## Issues Fixed

### 1. Auto Page Break (Deleted - Feature Not Needed)
- Hapus `useAutoPageBreak`, `_wrapOnChange`, `AUTO_PAGE_BREAK_PATTERN`
- Files: `.hook.ts`, `.component.tsx`, `.config.ts`, `.type.ts`, `index.ts`

### 2. Tab Key Lost After Reload
- Root cause: Clipboard regex `/[^\S\u00a0]/g` converts `\t` to space
- Fix: Custom Tab binding inserts `\u00a0\u00a0\u00a0\u00a0` (nbsp) + safeValue backward compat

### 3. Dual Language Border Appears After Load
- Root cause: `borderless-table` class on wrapper `<div>` lost during clipboard conversion
- Fix: register `border-color` StyleAttributor, inject inline `border-color: transparent` on `<td>`, CSS `td:has([style*="border-color"])`

## Worktree
- Created: IIAU-778-bug-quil-editor (from feature/components)
- Path: `/var/www/html/project-asum/IIAU-778-bug-quil-editor`
- Branch: `fix/IIAU-778-bug-quil-editor`
