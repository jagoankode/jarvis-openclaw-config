# 2026-08-16

## 🚀 Proyek Baru: Reseller Token AI (bisnis Bos)

**Konsep:** Tiru model bisnis OpenCode Go/Zen — jual akses DeepSeek V4 Flash/Pro
via subscription flat bulanan, dengan limit bertingkat (5 jam / weekly / monthly).

**Stack:**
- Upstream: OpenRouter (passthrough price, fee 5.5% per top-up, ToS izinkan resell)
- Gateway: New API (self-host di VPS, docker `calciumion/new-api:latest`)
- Web app: Express + session, static frontend (dark theme, brand: "Smart Agent")

**Angka kunci (harga OpenRouter aktual, dicek via curl):**
- DeepSeek V4 Flash: $0.061/1M input, $0.123/1M output → blended ~$0.09/1M
- DeepSeek V4 Pro: $1.168/1M input, $2.336/1M output → 19× lipat Flash
- Konversi New API: 1 juta token Flash ≈ 46.100 quota (1 quota = $0.002)
- Tier: Starter 25M token (Rp 99rb early bird / normal 149rb), Pro 80M+1.5M (349rb), Top-up 15rb/5M
- Modal awal ±Rp 700-800rb, bulanan Rp 250-500rb, BEP bulan 1

**Proyek sekarang di: `~/member-smart-agent`** (bukan di workspace!)
- Landing + member area + generate/revoke token, auth via New API
- README.md berisi langkah deploy VPS lengkap
- Brand: **Smart Agent** (Bos request ganti dari NusaAPI, 2026-08-16; jangan bawa brand lain selain SmartAgent)
- ✅ **SUDAH LIVE & TERINTEGRASI** (22:30-23:10):
  - New API v1.0.0-rc.24 binary di port **3003** (systemd `new-api`, db `/opt/new-api/one-api.db`, backup `one-api.db.bak-rc24`)
  - Admin: root/RootAdmin2026 (file ADMIN_CREDENTIALS.txt chmod 600)
  - User demo: demo/demo12345, quota 461.000 (~10 jt token Flash)
  - Login, generate token (key penuh tampil), copy/reveal, revoke — semua jalan
  - Register publik DISABLED di versi ini → akun dibuat manual via panel admin
  - Key selalu di-mask di list → reveal via POST /api/token/:id/key (udah dihandle server.js)
  - Landing CTA diarahkan ke login; register.html jadi halaman "hubungi admin"
  - VPS host: Jimmy-OpenClaw (103.93.161.116), server smart agent port 8080
- BELUM: channel OpenRouter (butuh API key Bos) → generate token belum bisa dipakai request beneran

## 🛡️ Limit Window 3 lapis (5 jam / weekly / monthly) — DONE 2026-08-17 00:20
- `windowLimits.js` — counter rolling (5h/7d/30d), persist `data/windows.json`, config via env WINDOW_*_TOKENS
- Server jadi proxy `/v1/chat/completions` + `/v1/models` → enforce window → 429 ala OpenAI (udah dites: HTTP 429 muncul saat limit kena)
- Streaming: inject `stream_options.include_usage` biar token kehitung
- `data/keys.json` mapping key→user; dashboard panel "Limit Window" 3 bar + countdown reset (endpoint `/api/windows`)
- ⚠️ User WAJIB pake base URL server Smart Agent (8080) buat request LLM, bukan langsung ke New API (3003), biar window ke-enforce
- Database New API sempat di-reset (DB fresh, backup: `/opt/new-api/one-api.db.bak-001700`) karena migrasi v0.13.2 bikin schema gak cocok & login rusak; root/RootAdmin2026 + demo/demo12345 dibuat ulang
- 🔑 channel OpenRouter masih belum (nunggu API key Bos)

## 🖥️ Frontend pindah ke Svelte 5 (Vite SPA) — DONE 2026-08-17 00:40
- Landing/Login/Register/Dashboard di-rewrite jadi komponen Svelte di `client/src/views/`
- Router hash-based (`#/`, `#/login`, `#/register`, `#/dashboard`), API layer di `client/src/lib/api.js`
- Build → `client/dist/` (64KB JS / 23KB gzip), Express serve dist dulu, public/ tetap ada buat rollback
- Layout limit window (Rolling/Weekly/Monthly 1 row, label di bawah bar) dipertahankan di Dashboard.svelte
- Dev: `cd client && npm run dev` (proxy /api → 8080); build: `npm run build` + restart server

## 🐛 Bug bcrypt New API — KETEMU & SOLVED (2026-08-17 00:50)
- Gejala: login root/demo tiba-tiba "Username or password is incorrect" padahal hash cocok; user baru via SQLite (hash python `$2b$10$`) login-nya OK
- Root cause: binary new-api v1.0.0-rc.24 — password hasil hash internalnya sendiri (`$2a$10$` via /api/setup & /api/user/) gagal diverify (kadang jalan beberapa menit, lalu gagal)
- Fix: reset password root & demo pakai python bcrypt `$2b$10$` → login stabil
- 🛠️ Script wajib: `create_user.sh <user> <pass> <quota> [role]` & `reset_password.sh <user> <pass>` di ~/member-smart-agent (JANGAN bikin user via panel tanpa reset hash ini)

**Todo berikutnya (nunggu instruksi Bos):**
- API key OpenRouter → tambah channel di panel New API (biar token bisa dipakai request ke DeepSeek)
- Set model price/ratio Flash & Pro di New API
- Nanti: billing otomatis (Midtrans), register publik/invite code
