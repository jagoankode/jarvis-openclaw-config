# Session: 2026-08-22 11:18:55 GMT+7

- **Session Key**: agent:main:telegram:velma:direct:8614405264
- **Session ID**: d500727a-0aca-4554-afb5-148695215a79
- **Source**: telegram

## Conversation Summary

user: jelaskan tentang k8s v1.37 dan gateway api v1.6
assistant: D
assistant: D
ari report riset + pengetahuan umum:

**⚙️ Kubernetes v1.37** (rilis ~Sep 2026)
- **Deprecate kube-proxy mode `ipvs`** → bakal dihapus di v1.43. Alasan: mode `iptables`/`nftables` makin mature. Cek setup kamu: di kube-proxy config.
- **Static Pods dilarang referensi Secrets/ConfigMap** → enforcement baru. Kalau selama ini static pod inject secret, harus refactor (mount file statis / kubelet credential).
- **cgroup v1 fase keluar** → `failCgroupV1` default true. Fitur resource canggih (kualitas QoS, isolasi memori) butuh cgroup v2. Cluster lama (CentOS 7/8, Ubuntu <22.04) bakal gagal start kalo masih v1.
- **Audit config cluster wajib sebelum upgrade** — banyak breaking change kumulatif.

**🌐 Gateway API v1.6**
- **TCPRoute & UDPRoute → GA (standard)** — ini yang paling signifikan. Routing L4 (DB, DNS, VoIP, game server) jadi first-class, tanpa CRD vendor (nggak perlu depend NGINX/HAProxy/Envoy specific endpoint lagi).
- **Resource eksperimental dipisah** ke group `gateway.networking.x-k8s.io` → stabil vs eksperimen jelas, tidak ganggu API stabil.

**Relevansi ke kamu:** kalo kelola cluster/GPU AI — siapkan cgroup v2 + DRA sebelum scale. Kalo bangun API gateway / network service — Gateway API v1.6 udah bisa jadi standar L4+L7 sekaligus.

Mau kubantu: cek versi cluster kamu (kube-proxy mode? cgroup?), atau breakdown pakai Gateway API buat L4 routing?
